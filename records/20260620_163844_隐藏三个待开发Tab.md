# 隐藏三个待开发 Tab 页面

- **时间**: 2026-06-20 16:38
- **任务**: 隐藏 Web 页面上的"特征聚类"、"零样本检测"、"少样本检测"三个 Tab，待后续开发
- **背景**: 这三个功能尚未开发完成，先隐藏前端入口，减少用户混淆，降低未完成功能被误触的可能

## 修改文件

**frontend/index.html**

### 修改内容

1. **Tab 按钮 (line 717-719)**: 为三个按钮添加 `style="display:none"`
   - `📊 特征聚类` -> cluster
   - `🎯 零样本检测` -> zero-shot
   - `🔬 少样本检测` -> few-shot

2. **内容区域 (line 1021, 1138, 1275)**: 为三个 content div 添加 `style="display:none"`
   - `<div id="cluster" class="content">`
   - `<div id="zero-shot" class="content">`
   - `<div id="few-shot" class="content">`

3. **注释标记**: 在注释中标注 "(已隐藏，待后续开发)" 方便后续查找

### 未改动

- **JS 事件绑定**: `initClusterPage()`, `initZeroShot()`, `initFewShot()` 等初始化代码保持不变
- **JS 变量**: `clusterSelectedFiles`, `zeroShotFiles`, `fewShotRefFiles` 等状态变量保留
- **switchTab 逻辑**: cluster/zero-shot/few-shot 分支保留 — 即使按钮隐藏，通过其他方式调用也不会报错
- 所有后端 API 和功能代码保持完整

### 恢复方法

删除对应元素上的 `style="display:none"` 即可恢复显示：
- 搜索 `style="display:none" onclick="switchTab('cluster')"` 等按键
- 搜索 `<div id="cluster" class="content" style="display:none">` 等内容区域
