# 待办任务执行记录

## 概述

执行时间：2026-07-07
基于 `records/docs/需求设计.md` 中的待办任务执行方案。

---

## Task 2: 同步 Anomalib / ADer 与 GitHub 最新版

### 2.1 Anomalib v2.2.0dev → v2.5.0 ✅

**操作：**
1. 通过 pip 下载 anomalib==2.5.0 wheel 包
2. 解压并替换 `algorithms/Anomalib/` 整个目录
3. 将内部 import 从 `from anomalib.` 批量替换为 `from Anomalib.`（保持目录大写）
4. 修复 `utils/path.py` 中的 `get_pretrained_weights_dir()`，使其优先返回项目根目录的 `models/pre_trained`
5. 更新 `anomalib_adapter.py`，注册 8 个新算法
6. 更新 `config.yaml`，添加 8 个新算法的配置条目和模型路径

**新增算法（8个）：** anomalyvfm, cfm, general_ad, glass, inp_former, l2bt, patchflow, anomaly_dino

**涉及文件：**
- `algorithms/Anomalib/` — 整个目录替换为 v2.5.0
- `algorithms/Anomalib/utils/path.py` — 修复路径指向项目目录
- `algorithms/anomalib_adapter.py` — 新增 8 个算法注册
- `backend/config/config.yaml` — 新增算法配置和模型路径

### 2.2 ADer 同步到上游最新版 ✅

**操作：**
1. 从 `https://github.com/zhangzjn/ADer` 克隆最新代码
2. 保留项目自定义的 `__init__.py`（包含 ADerTaskAssigner 类）
3. 保留项目自定义的 SPK 数据集配置文件（`*_spk.py`）
4. 替换其余所有核心文件（model/, trainer/, loss/, optim/, configs/, data/, util/ 等）

**涉及文件：**
- `algorithms/ADer/model/`, `trainer/`, `loss/`, `optim/`, `configs/` 等 — 替换为上游最新版
- `algorithms/ADer/__init__.py` — 保留项目自定义版本

---

## Task 3: 集成 Dinomaly2 ✅（从独立仓库完成）

**2026-07-08 更新:** 用户指出 Dinomaly2 是独立仓库 [guojiajeremy/Dinomaly2](https://github.com/guojiajeremy/Dinomaly2)（非 Dinomaly 的 dinomaly2 分支）。

**已完成：**
1. 下载完整 Dinomaly2 源码到 `algorithms/Dinomaly2/`
2. 创建 `algorithms/__init__.py` package init
3. 重写 `algorithms/dinomaly2_adapter.py` — 完整推理适配器:
   - `Dinomaly2Inference` 推理引擎类
   - 支持 DINOv2 Small/Base/Large 三种骨干网络
   - 完整的模型构建（encoder + bottleneck + decoder）
   - Context-Aware Recentering、Linear Attention、Loose Constraint
   - 异常图计算（编码器-解码器余弦距离）
   - 注册 3 个变体: dinomaly2_dinov2_{small,base,large}
4. 更新 `config.yaml` 添加 Dinomaly2 模型路径
5. 修复路径：Dinomaly2 内部平铺导入自动解析

---

## Task 5: 模型训练支持各类模型 ✅（后端重构完成）

### 操作
1. 重构 `backend/api/training.py`：
   - 新增 `ALGORITHM_FAMILIES` 定义支持的算法族（dinomaly/anomalib/ader）
   - 扩展 `TrainingRequest` 添加 `algorithm_family` 和 `algorithm_name` 字段
   - 新增 `GET /api/training/families` 端点返回支持的算法族
   - 新增 `_dispatch_training()` 根据算法族分派到不同训练执行器
   - 新增 Dinomaly、Anomalib、ADer 三个训练执行器
   - 通用子进程日志管理 `_run_subprocess_with_logging()`
   - 扩展 `TrainedModel` 和 `TrainingStatus` 添加 `algorithm_family` 字段
   - `GET /models` 从文件名推断算法族

2. 更新 `algorithms/factory.py`：
   - `custom:` 前缀解析扩展支持 anomalib/ader 文件名格式

3. 更新 `backend/api/custom_detection.py`：
   - 算法分组新增 Dinomaly2 和 Anomalib v2.5.0 算法

### 待完成（前端）
- `frontend/training.html` 添加算法族选择器
- 当前仅后端重构完成，前端需后续更新

---

## 当前状态统计

| 指标 | 数值 |
|------|------|
| 总注册算法数 | 69 |
| Anomalib 版本 | v2.5.0 |
| 支持的训练算法族 | 3（Dinomaly / Anomalib / ADer） |
| 修改/新增文件 | 12 |

---

## 待继续

1. **Task 4: Benchmark 功能** — 新增后端 API + 前端 Tab
2. **Training 前端更新** — training.html 添加算法族选择器
3. **Dinomaly2 算法** — 等待官方代码发布后补充推理逻辑
