要求:
项目环境在 .venv
不要用docker
要使用cuda,整理所有项目的库环境后再配置环境
将工作记录留痕, 在records文件夹中生成md文件存储工作记录(具体的时间戳,必须是真实的当前时间) ✅ 已完成 (2026-05-29)
服务日志文件在 logs 下
使用start_server.sh脚本开关服务
确认完成任务后更新本文档 ✅ 已更新
项目需要部署到服务器上,防止硬编码 ✅ 已修复
将算法需要用到的模型文件下载到本地


已完成:

✅ 消除硬编码路径 (2026-05-29)
  - start_server.sh: PROJECT_DIR 改为 $(cd "$(dirname "$0")" && pwd)
  - dinomaly_train_evaluate.py: --data_path 默认值改为动态检测项目根目录
  - start_server.py 和 backend/main.py 已使用动态路径检测

✅ 多用户并发场景支持 (2026-05-29)
  - 后端: backend/api/auth.py (用户认证和会话管理)
  - 登录: POST /api/auth/login, 登出: POST /api/auth/logout
  - 支持用户: admin, user1-user5 (密码: tp123456)
  - 会话过期时间: 24小时
  - 前端 login.html 使用真实API调用

✅ 集成数据集构建功能 (2026-05-29 完成, 2026-05-29 优化)
  - 数据集构建页: 用户可以对音频进行视听和判别异常/正常,根据用户的判别结果得到音频文件标签,并划分数据库
  - 前端: frontend/dataset.html (含波形图+时频图可视化、Label Studio 风格标注界面)
  - 后端: backend/api/dataset_builder.py
  - 数据来源: 自动匹配上传、手动上传、手动切分
  - 数据存储: data/spk/{category}/train|test/{label}/
  - 划分比例: 训练集:测试集 = 10:1
  - 时频图: 10种配色方案，Canvas直接渲染

✅ 新增模型训练功能 (2026-05-29 完成)
  - 前端: frontend/training.html (Chart.js 统计图表)
  - 支持选择训练集、dinomaly dinov2/v3 编码器配置
  - 后台子进程运行训练，实时日志监控

✅ 自训练模型集成到检测流程 (2026-05-29)
  - 算法选择框分为"预置模型"和"自训练模型"两个分组
  - factory.py 自动识别 custom: 前缀

✅ 任务管理清理修复 (2026-05-30)
  - 硬编码相对路径改为基于 project_root 的动态绝对路径

✅ 特征聚类可视化修复 (2026-05-30)
  - result_dir 改为绝对路径，output/vis/ → visualize/ 路径转换

✅ 全面消除硬编码相对路径 (2026-05-30)
  - 修复12个文件共36处，全部改为基于 PROJECT_ROOT 的动态绝对路径

✅ 离线检测历史记录修复 (2026-06-16)
  - 问题: 历史记录恢复时音频路径未验证文件是否存在
  - 修复: 异步验证文件存在性，不存在则删除记录

✅ Shazam 内存数据库迁移 (2026-06-20)
  - 将 MySQL 依赖的指纹库迁移到进程内内存数据库
  - 新增 InMemoryConnector + _MemDB 线程安全单例
  - 兼容 mode: AudioFingerprinter(backend="mysql") 仍可用

✅ 数据存储路径分析 (2026-06-20)
  - 梳理全部模块的数据目录、存储方式
  - 工作记录: records/20260620_161238_数据存储路径分析.md

✅ 隐藏待开发 Tab (2026-06-20)
  - 特征聚类、零样本检测、少样本检测 三个 Tab 隐藏，保留 JS 逻辑

✅ index.html 拆分 + 任务管理增强 (2026-06-20)
  - 5799 行 → 982 行，CSS 和 JS 拆分为独立模块文件
  - 任务管理: 统计卡片、筛选、分页、进度条、删除

✅ 任务管理支持在线检测 (2026-06-20)
  - 新增 /api/client/results、/api/client/stats、DELETE /api/client/results/{result_id}
  - 前端同时显示离线任务和在线结果，按时间合并排序

✅ 参考音频支持试听 (2026-06-20)
  - 新增 GET /api/reference-audio/audio/{music_id} 端点
  - 前端表格增加试听按钮

✅ 数据集构建功能完善 (2026-06-20)
  - 三种数据来源: 手动上传、离线检测、在线检测
  - V2 会话管理: 持久化、预览、标注、确认导入
  - 详见工作记录: records/20260620_数据集构建功能完善.md


== 详细完成记录 ==

以下是各次任务的具体修改详情，按时间倒序排列。

### 数据集构建功能完善 (2026-06-20)

**来源**: 需求.txt 待办
**改造方案**:
1. V2 数据集构建器 (dataset_builder_v2.py) 新增三个后端端点：
   - POST /from-client-detection — 从在线/客户端检测结果创建构建会话
   - GET /available-tasks — 获取已完成离线任务列表
   - GET /available-client-results — 获取在线检测结果列表
2. V2 会话持久化：模块加载时从 data/dataset_builder/*.json 恢复会话
3. main.py 注册 V2 路由 + data/dataset_builder 静态文件挂载
4. 前端 dataset.html 新增：
   - 数据来源 Tab（手动上传/离线检测/在线检测）
   - 离线任务浏览面板（选择 → 导入 → 构建会话）
   - 在线结果浏览面板（多选 → 导入 → 构建会话）
   - V2 会话管理面板（预览/标注/确认导入正式数据集）
   - 标注按钮同时支持 V1 和 V2 的标注端点
5. 数据流：三种来源 → V2 DatasetBuildSession（预标注保留） → 人工标注 → data/spk/
6. 修改文件:
   - backend/api/dataset_builder_v2.py (新增端点+持久化)
   - backend/main.py (注册V2路由+静态挂载)
   - frontend/dataset.html (前端UI+JS)
7. 工作记录: records/20260620_数据集构建功能完善.md

### 参考音频支持试听 (2026-06-20 17:45)

**改造原因**: 参考音频库只有 ID/名称/哈希数/删除 等基本信息，无法试听音频内容
**后端改造**:
- backend/api/reference_audio.py: 新增 GET /api/reference-audio/audio/{music_id} 端点
- 上传流程增加保存音频文件到 data/ref/ 目录
**前端改造**:
- frontend/index.html: 参考音频表格增加"试听"列
- frontend/js/reference-audio.js: 新增 toggleRefAudioPlay() 播放函数

### 任务管理支持在线检测 (2026-06-20 17:25)

**改造原因**: 任务管理 Tab 原本仅显示离线检测任务
**后端改造**:
- client_monitor_service.py: 新增 get_results/get_results_count/delete_result 分页查询
- client_monitor.py: 新增 GET /api/client/results、/api/client/stats、DELETE /api/client/results/{result_id}
**前端改造**:
- frontend/index.html: 筛选栏增加"类型"下拉框，表格增加"类型"和"客户端"列
- frontend/js/task-manager.js: 支持同时加载离线任务和在线结果，按时间合并排序

### index.html 拆分 + 任务管理增强 (2026-06-20 17:00)

**改造原因**: index.html 达 5799 行、279KB，维护困难
**拆分方案**:
- CSS → frontend/css/index.css
- JS → 9 个独立模块文件
- index.html: 5799 行 → 982 行
**任务管理增强**: 统计卡片、筛选栏、进度条、分页、单任务删除、任务耗时、统计联动

### 隐藏待开发 Tab (2026-06-20 16:38)

**修改**: 特征聚类、零样本检测、少样本检测 Tab 添加 `style="display:none"`
**恢复**: 删除对应元素上的 style 即可

### 数据存储路径分析 (2026-06-20 16:12)

**分析范围**: 全部模块的存储路径和方式
**工作记录**: records/20260620_161238_数据存储路径分析.md

### Shazam 内存数据库迁移 (2026-06-20)

**改造原因**: 部署到无 MySQL 的环境会直接失败
**第一期** (14:58):
- 新增 in_memory.py — InMemoryConnector + _MemDB(线程安全单例)
- AudioFingerprinter 默认 backend="memory"

**第二期** (15:23):
- 修复仍直接使用 MySQLConnector 的外部模块
- parallel.py、matching_engine.py、locator.py、client_monitor_service.py、local_monitor_service.py
- 兼容性: AudioFingerprinter(backend="mysql") 仍可用

### 离线检测历史记录修复 (2026-06-16)

**问题原因**: 历史记录恢复时音频路径未验证文件是否存在
**修复方案**: 异步验证文件存在性，不存在则删除记录
**修改文件**: frontend/index.html
**修改内容**:
1. loadOfflineResultsFromStorage() 改为异步函数
2. 新增 validateAndCleanHistoryRecords() 函数
3. 音频文件不存在 → audio_slice_path 设为 null
4. 热力图文件不存在 → 相关路径设为 null
5. 都文件不存在 → 删除该记录
6. 清理后保存到 localStorage

### 全面消除硬编码相对路径 (2026-05-30)

修复12个文件共36处硬编码相对路径，全部改为基于 PROJECT_ROOT 的动态绝对路径。
涉及: few_shot.py, zero_shot.py, feature_cluster.py, local_monitor.py, client_monitor.py,
detection.py, local_monitor_service.py, client_monitor_service.py, task_manager.py,
config_load.py, config_manager.py

### 特征聚类可视化修复 (2026-05-30)

- result_dir 改为基于 project_root 的绝对路径
- 增加 output/vis/ → visualize/ 的路径转换分支

### 任务管理清理修复 (2026-05-30)

cleanup_old_tasks() 中的硬编码相对路径改为基于 project_root 动态检测的绝对路径。

### 自训练模型集成到检测流程 (2026-05-29)

- GET /api/detection/algorithms 同时返回预置模型和自训练模型
- 算法选择框分为"预置模型"和"自训练模型"分组
- factory.py 自动识别 custom:前缀

### 数据集构建和模型训练集成到主页面 (2026-05-29)

- 通过 iframe 嵌入到 index.html 的 tab 面板中
- 与主页面保持一致的宽度

### 模型训练功能 (2026-05-29)

- 前端: training.html (Chart.js 图表)
- 支持 dinomaly dinov2/v3 配置
- 后台子进程运行，实时日志监控
- 训练脚本以模块方式运行

### 数据集构建功能 (2026-05-29)

- 前端: dataset.html (WaveSurfer 可视化 + 时频图)
- 后端: dataset_builder.py (上传、切分、标注、统计)
- 数据存储: data/spk/{category}/train|test/{label}/

### 多用户支持 (2026-05-29)

- 用户认证和会话管理 (auth.py)
- 支持 admin, user1-user5

### 消除硬编码路径 (2026-05-29)

- start_server.sh: 动态检测项目根目录
- dinomaly_train_evaluate.py: --data_path 动态默认值
