要求:
项目环境在 .venv
不要用docker
要使用cuda,整理所有项目的库环境后再配置环境
将工作记录留痕, 在records文件夹中生成md文件存储工作记录(具体的时间戳,必须是真实的当前时间)
服务日志文件在 logs 下
确认完成任务后更新本文档
项目需要部署到服务器上,防止硬编码
将算法需要用到的模型文件下载到本地


已完成:
1. ~~启动服务后,在离线检测页面恢复 1 条历史检测结果,但是无法播放和显示频谱图,检查一下是为什么~~ [已完成 - 2026-06-16]
   - 问题原因: 历史记录恢复时，音频路径是从热力图路径推断出来的，没有验证文件是否真实存在
   - 修复方案: 在加载历史记录时，添加异步验证文件是否存在的逻辑，如果所有文件都不存在则删除该记录
   - 修改文件: frontend/index.html
   - 修改内容:
     1. 将 loadOfflineResultsFromStorage() 改为异步函数
     2. 新增 validateAndCleanHistoryRecords() 函数，使用 HEAD 请求验证音频文件和热力图文件是否存在
     3. 如果音频文件不存在，将 audio_slice_path 设为 null
     4. 如果热力图文件不存在，将相关路径设为 null
     5. 如果音频和热力图文件都不存在，从离线结果数据中删除该记录
     6. 清理后保存到 localStorage

2. ~~shazam算法强依赖MySQL(pymysql)指纹库数据库,迁移到进程内内存数据库 (backend.core.shazam.database.in_memory)~~ [已完成 - 2026-06-20]
   - 改造原因: 部署到无 MySQL 的环境会直接失败
   - 第一期 (14:58):
     1. backend/core/shazam/database/in_memory.py (新增) — InMemoryConnector + _MemDB(线程安全单例) + InMemoryDatabaseChecker
     2. backend/core/shazam/api.py: AudioFingerprinter 默认 backend="memory"
     3. backend/core/shazam/database/__init__.py: 导出 InMemory*
   - 第二期 (15:23) — 修复仍直接使用 MySQLConnector + 原始 SQL 的外部模块:
     1. database/connector.py: IConnector 新增 get_fingerprints_by_music_id / get_all_music 抽象方法
     2. database/in_memory.py: _MemDB + InMemoryConnector 实现两新方法
     3. backend/core/shazam/parallel.py: 补齐 ThreadLocalFingerprinter / ParallelAudioFingerprinter / 便捷函数的 backend 参数
     4. backend/core/long_audio_analyzer/matching_engine.py: build_index_from_database() 原始SQL→接口方法
     5. backend/core/precise_segment_locator/locator.py: 默认 InMemoryConnector, 3处原始SQL→接口方法
     6. backend/core/client_monitor_service.py: MySQLConnector→InMemoryConnector, 原始SQL→get_all_music()
     7. backend/core/local_monitor_service.py: 两处同上 (_init_analyzer + _update_reference_audios)
     8. backend/core/shazam/create_database.py: 修复导入路径
   - 兼容性: AudioFingerprinter(backend="mysql") 仍可用
   - 切换回 MySQL: AudioFingerprinter(backend="mysql")

3. ~~分析整理一下当前项目中需要用到的数据和存储路径 存储方式~~ [已完成 - 2026-06-20 16:12]
   - 分析范围: 项目全部模块（Shazam指纹库、模型文件、参考音频、数据集、上传文件、输出文件、日志、配置、前端存储等）
   - 存储路径: 逐一列出所有数据目录的绝对/相对路径和用途
   - 存储方式: 内存+JSON持久化（指纹库）、文件系统（模型/音频/图片）、YAML（配置）、localStorage（前端）
   - 工作记录: records/20260620_161238_数据存储路径分析.md



已完成:
4. ~~将web上的特征聚类 零样本检测 少样本检测 三个tab隐藏,后续再开发~~ [已完成 - 2026-06-20 16:38]
   - 修改方案: 在三个 tab 按钮和对应的 content div 上添加 `style="display:none"`，保留 JS 逻辑不变
   - 修改文件: frontend/index.html
   - 修改内容:
     1. 第 717-719 行：三个 tab 按钮添加 `style="display:none"`
     2. 第 1021 行：cluster 内容区添加 `style="display:none"`
     3. 第 1138 行：zero-shot 内容区添加 `style="display:none"`
     4. 第 1275 行：few-shot 内容区添加 `style="display:none"`
     5. 注释标记添加 "(已隐藏，待后续开发)" 提示
   - 恢复方法: 删除对应元素上的 `style="display:none"` 即可
   - 工作记录: records/20260620_163844_隐藏三个待开发Tab.md
5. ~~拆分 index.html 到多文件 + 增强任务管理 Tab 功能~~ [已完成 - 2026-06-20 17:00]
   - 改造原因: index.html 达 5799 行、279KB，维护困难；任务管理功能太少
   - 拆分方案:
     1. CSS 提取到 frontend/css/index.css（原 680 行内联样式）
     2. 内联 JS（~4200 行）拆分为 9 个独立模块文件
   - 新建文件:
     - frontend/css/index.css — 全部样式
     - frontend/js/app.js — 核心模块（登录、API、弹窗、Tab切换、音频播放、设备/参考音频加载）
     - frontend/js/offline-detection.js — 离线检测
     - frontend/js/realtime-monitor.js — 实时监控（隐藏保留）
     - frontend/js/task-manager.js — 任务管理（增强版）
     - frontend/js/reference-audio.js — 参考音频库管理
     - frontend/js/client-monitor.js — 客户端监控
     - frontend/js/cluster.js — 特征聚类（隐藏保留）
     - frontend/js/zero-shot.js — 零样本检测（隐藏保留）
     - frontend/js/few-shot.js — 少样本检测（隐藏保留）
   - index.html: 5799 行 → 982 行（仅保留 HTML 骨架）
   - 任务管理增强:
     1. 统计卡片（总/运行中/排队/已完成/失败，使用 /api/tasks/stats）
     2. 筛选栏（按状态/算法/搜索任务ID）
     3. 进度条可视化
     4. 分页（后端已支持 limit/offset）
     5. 单任务删除（使用 DELETE /api/tasks/{task_id}）
     6. 结构化任务详情（内嵌于弹窗，显示文件级结果）
     7. 任务耗时显示
     8. 统计卡片点击筛选联动
   - 工作记录: records/20260620_170000_index.html拆分与任务管理增强.md




待办:
6. ~~修改任务管理模块,使之也能适用于在线检测的任务~~ [已完成 - 2026-06-20 17:25]
   - 改造原因: 任务管理 Tab 原本仅显示离线检测任务，在线检测结果需切换到单独页面查看
   - 后端改造:
     1. backend/core/client_monitor_service.py: 新增 get_results/get_results_count/delete_result 分页查询方法，为每条结果添加 result_id 自增标识和 algorithm 字段
     2. backend/api/client_monitor.py: 新增 GET /api/client/results (分页列表)、GET /api/client/stats (统计)、DELETE /api/client/results/{result_id} (删除单条) 三个端点
   - 前端改造:
     1. frontend/index.html: 移除"仅显示离线检测任务"提示框，筛选栏增加"类型"下拉框(全部/离线任务/在线检测)，表格增加"类型"和"客户端"列
     2. frontend/js/task-manager.js: 全面重构，支持同时加载离线任务(/api/tasks/list)和在线结果(/api/client/results)，按时间合并排序后统一渲染，类型徽章区分，在线结果仅显示"详情"和"删除"操作
   - 工作记录: records/20260620_172517_任务管理支持在线检测.md

7. ~~参考音频列表中应能支持参考音频试听~~ [已完成 - 2026-06-20 17:45]
   - 改造原因: 参考音频库只有 ID/名称/哈希数/删除 等基本信息，无法试听音频内容
   - 后端改造:
     1. backend/api/reference_audio.py: 新增 GET /api/reference-audio/audio/{music_id} 端点，返回音频文件(FileResponse)，支持 wav/mp3/flac 等格式
     2. backend/api/reference_audio.py: 上传流程增加保存音频文件到 data/ref/ 目录（之前仅保存指纹，文件已被删除）
   - 前端改造:
     1. frontend/index.html: 参考音频表格增加"试听"列
     2. frontend/js/reference-audio.js: 新增 toggleRefAudioPlay() 播放函数（独立播放状态），渲染函数增加试听按钮
   - 工作记录: records/20260620_174541_参考音频支持试听.md