要求:
项目环境在 .venv
不要用docker
要使用cuda,整理所有项目的库环境后再配置环境
将工作记录留痕, 在records文件夹中生成md文件存储工作记录(具体的时间戳,必须是真实的当前时间) ✅ 已完成 (2026-05-29)
服务日志文件在 logs 下
使用start_server.sh脚本开关服务
确认完成任务后更新本文档 ✅ 已更新 (2026-05-29)
项目需要部署到服务器上,防止硬编码 ✅ 已修复 (2026-05-29: start_server.sh 和 dinomaly_train_evaluate.py 使用动态路径)


已完成:
✅ 消除硬编码路径 (2026-05-29)
  - start_server.sh: PROJECT_DIR 改为 $(cd "$(dirname "$0")" && pwd)
  - dinomaly_train_evaluate.py: --data_path 默认值改为动态检测项目根目录
  - start_server.py 和 backend/main.py 已使用动态路径检测

✅ 多用户并发场景支持 (2026-05-29)
  - 后端: backend/api/auth.py (用户认证和会话管理)
  - 登录: POST /api/auth/login, 登出: POST /api/auth/logout
  - 支持用户: admin, user1-user5 (密码: tp123456)
  - 会话过期时间: 24小时
  - 前端 login.html 使用真实API调用

✅ 集成数据集构建功能 (2026-05-29 完成, 2026-05-29 优化)
  [1] 数据集构建页: 用户可以对音频进行视听和判别异常/正常,根据用户的判别结果得到音频文件标签,并划分数据库.
    - 前端: frontend/dataset.html (含波形图+时频图可视化、Label Studio 风格标注界面)
    - 后端: backend/api/dataset_builder.py
  [2] 数据来源有三种:
    - 自动匹配上传: POST /api/dataset/upload-and-split (Shazam 自动匹配参考音频并切分)
    - 手动上传: POST /api/dataset/upload-manual (仅上传音频，后续手动选择参考音频切分)
    - 手动切分: POST /api/dataset/split-manual (对已上传音频指定参考音频进行切分)
  [3] 临时数据存储: uploads/dataset_temp/ 和 uploads/dataset_slices/ (共享目录，非用户隔离)
  [4] 数据库存储在data/spk目录下,按参考音频划分文件夹
    - 结构: data/spk/{category}/train/good/, test/good/, test/anomaly/
  [5] 新增数据按训练集:测试集 = 10:1 比例划分
  [6] 音频数据统计信息: GET /api/dataset/stats
  [7] 时频图美化 (2026-05-29):
    - 蓝紫配色方案（默认），共10种经典音频时频图配色可切换
    - 配色方案: 蓝紫、Audition、SoX、Praat、冷暖、彩虹、铜色、骨色、热力、灰阶
    - 直接渲染到Canvas保证清晰度，无中间缩放
    - 新增时间轴标签，频率网格线双层设计
    - 暗色控制栏主题，移除滚轮缩放功能

✅ 新增模型训练功能 (2026-05-29 完成)
  [1] 本地训练集状态可视化
    - 前端: frontend/training.html (Chart.js 柱状图+饼图)
  [2] 可选择训练集 (多选)
    - GET /api/training/datasets 返回可训练类别及其统计
  [3] 可选dinomaly dinov2/v3
    - 编码器类型: DINOv2 / DINOv3
    - 模型大小: small / base / large
    - 迭代次数和批次大小可配置
    - 后台子进程运行训练，实时日志监控
    - POST /api/training/start, GET /api/training/status/{task_id}
  [4] 训练脚本修复:
    - 以模块方式运行 (python -m algorithms.Dinomaly.dinomaly_train_evaluate)
    - 自动创建缺失的 ground_truth mask
    - 设置环境变量 (DINOMALY_ENCODER_DIR, PRETRAINED_MODELS_DIR, TORCH_HOME)

✅ 数据集构建和模型训练集成到主页面 (2026-05-29)
  - 数据集构建和模型训练通过 iframe 嵌入到 index.html 的 tab 面板中
  - 不再跳转到独立页面，与主页面保持一致的宽度 (max-width: 1400px)

✅ 自训练模型集成到检测流程 (2026-05-29)
  [1] GET /api/detection/algorithms 同时返回预置模型和 saved_results/ 中的自训练模型
  [2] 算法选择框分为"预置模型"和"自训练模型"两个分组
  [3] factory.py 自动识别 custom:前缀，从文件名解析模型类型和大小
  [4] 自训练模型可在离线和在线检测中直接选用

待办:
（无）


